#include <string>


// event
struct input {};

// guards
const auto is_help = (const input&)[]{};
const auto is_myport = (const input&)[]{};
const auto is_myip = (const input&)[]{};
const auto is_connect = (const input&)[]{};
const auto is_list = (const input&)[]{};
const auto is_terminate = (const input&)[]{};
const auto is_send = (const input&)[]{};
const auto is_exit = (const input&)[]{};

const auto help = (const input&)[]{};
const auto myport = (const input&)[]{};
const auto myip = (const input&)[]{};
const auto connect = (const input&)[]{};
const auto list = (const input&)[]{};
const auto terminate = (const input&)[]{};
const auto send = (const input&)[]{};
const auto exit = (const input&)[]{};
